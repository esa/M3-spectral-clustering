from setuptools import setup, find_packages

setup(name='speclearn',
      version='0.0',
      description='Machine learning with Moon Mineral Mapper (M3) spectral data',
      author='Freja Thoresen',
      author_email='freja.thoresen@esa.int',
      packages=find_packages(),
     )
